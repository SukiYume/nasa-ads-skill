interface:
  display_name: "NASA ADS"
  short_description: "Search ADS, export citations, metrics, and libraries"
  brand_color: "#0B3D91"
  default_prompt: "Use $nasa-ads to search astronomy papers, export citations, inspect metrics, manage libraries, or resolve full-text and data links."

policy:
  allow_implicit_invocation: true
